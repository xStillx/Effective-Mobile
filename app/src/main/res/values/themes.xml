<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EffectiveMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EffectiveMobile" parent="Base.Theme.EffectiveMobile" />

    <style name="BottomNavigation">
        <item name="android:background">@color/dark_gray</item>

        <item name="itemTextAppearanceInactive">@color/white</item>
        <item name="itemTextAppearanceActive">@color/green</item>
    </style>

    <style name="BottomNavTextStyle" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">@dimen/text_size_xs</item>
        <item name="fontFamily">@font/roboto_medium</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="ActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/light_gray</item>
    </style>
</resources>